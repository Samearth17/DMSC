# Existing X scraper integration

The original scraper was not supplied. No replacement X scraper is included.
Keep its files unchanged in an ignored local directory or separate repository.
Create a separate bridge module implementing the contract in `docs/X_ADAPTER.md`.
